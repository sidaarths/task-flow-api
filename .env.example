# Required
MONGODB_URI="mongodb://localhost:27017/taskflow"
JWT_SECRET="your-secret-key-minimum-32-characters-long!!"
NODE_ENV="development"

# Optional
PORT=3001
CLIENT_URL="http://localhost:3000"
LOG_LEVEL="debug"

# Removed: PUSHER_APP_ID, PUSHER_KEY, PUSHER_SECRET, PUSHER_CLUSTER
# Real-time is now handled via SSE (Server-Sent Events)
